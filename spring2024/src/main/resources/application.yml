spring:
  application:
    name: spring2024

  datasource:
    url: jdbc:h2:mem:todo;MODE=MYSQL

  h2:
    console:
      enabled: true

  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
